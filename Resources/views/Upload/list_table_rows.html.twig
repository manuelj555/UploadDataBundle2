{% for item in items %}
    <tr class="upload-row">
        {% for name, column in config.listMapper.columns|filter(v => is_show or v.options.use_show) %}
            {{ include(column.template, {
            item: item,
            column: column,
            }) }}
        {% endfor %}
    </tr>
{% endfor %}
