{% for item in items %}
    <tr class="upload-row">
        {% for name, column in config.listMapper.columns if not is_show or column.options.use_show %}
            {{ include(column.template, {
            item: item,
            column: column,
            upload_type: config.type,
            }) }}
        {% endfor %}
    </tr>
{% endfor %}
